# Supabase project used only for staff auth (session + JWT for the API).
VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>

# Base URL of api-server. Leave unset to call the API same-origin under /api.
VITE_API_URL=http://localhost:3001
